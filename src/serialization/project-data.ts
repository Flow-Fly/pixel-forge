// Compatibility boundary for client modules that predate the shared package.
export {
  hasProjectImageData,
  normalizeProjectFileImageData,
  normalizeProjectImageData,
} from '@pixel-forge/shared';
