{
  "name": "@pixel-forge/shared",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run --root .. tests/shared/project-contract.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  }
}
